import React from 'react'
import './App.css'
import { Button } from 'antd'
import 'mqtt'

type Props = {
};
export const App = (props: Props) => {
  return (
    <div className={'container'}>
      <Button type={'primary'}>
          aaa
      </Button>
    </div>
  )
}
